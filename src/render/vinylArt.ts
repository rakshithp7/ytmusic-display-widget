import { Dimensions, RenderOptions } from './types';

export function renderVinylArt(
  _thumbnailDataUri: string,
  _dimensions: Dimensions,
  _vinylSpeed: RenderOptions['vinylSpeed'],
  _labelSize: RenderOptions['labelSize']
): string {
  throw new Error('renderVinylArt is not implemented yet (see Task 6)');
}
